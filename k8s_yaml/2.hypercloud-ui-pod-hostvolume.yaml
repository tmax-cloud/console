apiVersion: apps/v1
kind: Deployment
metadata:
  name: console
  namespace: console-system
  selfLink: /apis/extensions/v1beta1/namespaces/console-system/deployments/console
spec:
  progressDeadlineSeconds: 600      
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      hypercloud: ui
      app: console
  template:
    metadata:
      name: hypercloud-ui
      labels:
        hypercloud: ui
        app: console
    spec:
      containers:      
      - command:
        - /opt/bridge/bin/bridge
        - --public-dir=/opt/bridge/static
        - --listen=https://0.0.0.0:6443
        - --tls-cert-file=/var/https-cert/tls.crt
        - --tls-key-file=/var/https-cert/tls.key
        - --login-endpoint=http://@@AUTH@@/login
        - --logout-endpoint=http://@@AUTH@@/logout  
        - --prometheus-endpoint=http://@@PROM@@/api
        image: 192.168.6.110:5000/hypercloud-console:@@TAG@@
        imagePullPolicy: IfNotPresent
        volumeMounts:
        - mountPath: /var/https-cert
          name: https-cert
          readOnly: true
        - mountPath: /var/console
          name: console-mnt
        name: console
        ports:
        - containerPort: 6443
          protocol: TCP
        resources:
          limits:
            cpu: "1"
            memory: "2Gi"        
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
      tolerations:
      - effect: NoSchedule
        key: node-role.kubernetes.io/master
      volumes:
      - name: https-cert
        secret:
          defaultMode: 288
          secretName: console-https-secret
      - name: console-mnt
        hostPath: 
          path: /root/hypercloud4.1-console/mnt/var
     
