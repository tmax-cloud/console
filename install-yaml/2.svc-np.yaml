apiVersion: v1
kind: Service
metadata:
  name: console-np
  namespace: console-test
spec:
  type: NodePort
  ports:
  - name: "https"
    port: 80
    targetPort: 6443
    nodePort: 31333
  selector:
    app: console   
    hypercloud: ui